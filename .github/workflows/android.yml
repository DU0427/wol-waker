name: build-android-apk

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'android/**'
      - 'package.json'
      - 'package-lock.json'
      - 'capacitor.config.ts'
      - '.github/workflows/android.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: 'npm'

      - name: Run core tests
        run: node src/core/core.test.mjs

      - uses: actions/setup-java@v6
        with:
          distribution: 'temurin'
          java-version: '17'

      # 必须 v4+：v4 起 action 迁到 Node 24，v3 被运行器强制升级后会失败
      - uses: android-actions/setup-android@v4

      - name: Install Android SDK 34
        run: |
          yes | sdkmanager --licenses > /dev/null 2>&1 || true
          sdkmanager --install "platforms;android-34" "build-tools;34.0.0" "platform-tools"

      - name: Install npm deps
        run: npm ci --no-audit --no-fund

      - name: Sync web assets into android project
        run: npm run cap:sync

      - name: Build debug APK
        working-directory: android
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Upload APK artifact
        uses: actions/upload-artifact@v7
        with:
          name: wol-waker-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

      - name: Publish APK to release (apk-latest)
        uses: softprops/action-gh-release@v3
        with:
          tag_name: apk-latest
          name: wol-waker 安卓测试包
          body: |
            自动构建的 debug 安装包，可直接在手机上安装测试。
            每次 main 分支构建都会覆盖更新此 Release。
          files: android/app/build/outputs/apk/debug/app-debug.apk
          fail_on_unmatched_files: true
